const express=require("express"),router=express.Router(),controller=require("../controller/file.controller.js");let routes=e=>{router.post("/upload",controller.upload),router.get("/files",controller.getListFiles),router.get("/files/:name",controller.download),e.use(router)};module.exports=routes;
